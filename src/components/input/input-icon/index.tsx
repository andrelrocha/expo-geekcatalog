import { styled } from "@gluestack-ui/themed";
import { InputIcon as GLInputIcon } from "@gluestack-ui/themed"; 

const InputIcon = styled(
    GLInputIcon,
    {
       
    },

);

export default InputIcon;